<h1>Tracks</h1>
<ul class="list">
    {{#each albumTracks}}
    <li>
        {{name}}
        <audio controls>
            <source src="{{preview_url}}">
        </audio>
    </li>
    {{/each}}
</ul>